/* FacultyAdminLayout — placeholder, will be implemented later */
import React from 'react';
import { Outlet } from 'react-router-dom';

const FacultyAdminLayout = () => {
  return <Outlet />;
};

export default FacultyAdminLayout;
