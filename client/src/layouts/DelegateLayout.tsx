/* DelegateLayout — placeholder, will be implemented later */
import React from 'react';
import { Outlet } from 'react-router-dom';

const DelegateLayout = () => {
  return <Outlet />;
};

export default DelegateLayout;
